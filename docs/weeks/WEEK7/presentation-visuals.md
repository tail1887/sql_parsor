# WEEK7 Team 3 — 자동 ID + B+ 인덱스 / 대규모 성능 비교

---

## 1) 구현 시퀀스

```mermaid
%%{init: {'theme':'base', 'themeVariables': {'fontSize':'22px', 'fontFamily':'system-ui, sans-serif'}}}%%
flowchart TB
    A["① B+ 트리 — 단독 모듈"]
    B["② 자동 id + payload"]
    C["③ INSERT 경로에 인덱스 연결"]
    D["④ Parser·AST — WHERE id = 정수"]
    E["⑤ SELECT 실행 분기"]
    F["⑥ 엣지·에러 처리"]
    G["⑦ 대량 벤치 + README"]

    A --> B --> C --> D --> E --> F --> G
```



---

## 2) 구현 핵심

```mermaid
flowchart LR
    subgraph leafrow["리프의 한 엔트리 (전형적 리프 한 칸)"]
        k["키: id (int64)"]
        p["payload (size_t)\n= CSV 데이터 행 인덱스"]
    end

    subgraph csv["테이블 본체 (CSV → 메모리 행)"]
        r0["rows[0]"]
        r1["rows[1]"]
        r2["rows[2]"]
    end

    p -->|"인덱스로 가리킴"| r2
```



---

## 3) `malloc/calloc` vs `mmap`

---


| 구분    | `malloc/calloc`             | `mmap`                    |
| ----- | --------------------------- | ------------------------- |
| 대상    | 프로세스 메모리                    | 파일/메모리 매핑                 |
| 연결    | `BPNode *` 포인터로 부모·자식·리프 연결 | page id, offset 관리 필요     |
| 구현 범위 | 메모리 B+ 트리 구현에 집중            | 파일 페이지, flush, 저장 형식까지 커짐 |
| 선택    | 이번 과제에 적합                   | 디스크 기반 DB 구현에 가까움         |


---

## 4) B+ 트리 구조 → B-tree와 비교

발표에서 **비교표만 먼저** 나가면 “B+가 뭔지”가 공중에 뜰 수 있어서, **순서는 `B+ 한 장 → 도식 → 비교표`** 를 권장한다.

### 4.0 B+ 트리를 한 줄로 (이번 과제·슬라이드 기준)

- 트리 안의 **키는 정렬**되어 있고, **실제로 인덱스가 가리키는 값(우리 구현: `id` → `payload` = CSV 행 인덱스)** 은 **리프에만** 둔다.
- **내부 노드**에는 “왼쪽·가운데·오른쪽 자식 중 어디로 갈지”를 나누는 **separator(복사 키)** 만 있고, **행 위치(payload)** 는 내부에 없다.
- **리프끼리 `next`로 연결**되면, 한 리프에서 시작해 **키 순서대로 옆 리프로** 이어 가며 **범위 스캔**하는 그림이 된다(개념도; 단건 `WHERE id =` 만 지원해도 구조 설명용으로 쓴다).

아래 **4.1 ~ 4.2** 는 위 세 줄을 **그림으로만** 보여 준다. **B-tree와의 차이**는 그 다음 **비교 표**에서 정리한다.

### 4.1 B+ 트리 — 내부 vs 리프 (개념만)

```
                        +------------------+
                        | 내부: 50 | 90    |  ← 라우팅용 복사 키
                        +--+--+--+--+--+--+
                           |     |     |
              +------------+     |     +------------+
              v                  v                  v
    +-------------------+ +-------------------+ +-------------------+
    | 리프: 10 20 30    | | 리프: 50 60 70    | | 리프: 90 100 …   |
    | pld pld pld       | | pld pld pld       | | pld  …            |  ← pld = payload(행 인덱스)
    +---------+---------+ +---------+---------+ +---------+---------+
              \-------------------+-------------------/
                        next 체인 (범위 스캔·개념도)
```

위 그림에서 **위쪽은 내부(라우팅만)**, **아래 세 박스는 리프(키 + payload)**. 화살표는 **루트에서 내려와 리프까지** 가는 탐색 경로, 아래 물결은 **리프 순서 스캔**을 뜻한다.

---

### 4.2 B+ 트리 — 리프 한 블록 (우리 코드와 대응)

```
  리프 (메모리)
  +------+------+------+
  | id:5 | id:8 | id:12|   ← 검색 키 (정렬)
  +------+------+------+
  |pld:0 |pld:1 |pld:2 |   ← payload (CSV 데이터 행 인덱스)
  +------+------+------+
```

**윗줄**은 검색에 쓰는 **키(`id`)**, **아랫줄**은 그 키에 대응하는 **payload**(CSV에서의 **행 번호**). 리프 한 칸 = **(키, payload)** 한 쌍이다.

---

### 4.3 B-tree vs B+ tree (한눈에 비교)

| 구분       | B-tree               | B+ tree                  |
| -------- | -------------------- | ------------------------ |
| 데이터 위치   | 내부 노드와 리프 노드 모두 가능   | 실제 key/payload는 리프에 모임   |
| 내부 노드 역할 | 데이터 저장 가능            | 길 안내용 separator key      |
| 리프 연결    | 필수 아님                | 리프끼리 `next`로 연결          |
| 강점       | 단건 검색에서 내부 노드 hit 가능 | 범위 검색, 정렬 순회, DB 인덱스에 유리 |


---

## 5) split 관점 차이 (B-tree vs B+ tree)

---


| 구분           | B-tree                 | B+ tree                        |
| ------------ | ---------------------- | ------------------------------ |
| 부모로 올라가는 key | 중간 key가 **이동**         | 오른쪽 리프 첫 key가 **복사**           |
| 아래 노드의 key   | 올라간 key가 빠질 수 있음       | 실제 key/payload는 리프에 남음         |
| 부모 key 의미    | 실제 데이터 key일 수 있음       | 탐색용 separator key              |
| 결과           | key/payload가 내부·리프에 분산 가능 | key/payload가 리프에 모여 range scan 유리 |

### 5.1 직관 다이어그램 — split 직후 구조

**B-tree:** overflow 시 **중간 키가 부모로 승격**하고, 그 키는 “구분자”로만 쓰이거나 아래 레벨에서는 빠질 수 있어 **내부·리프에 key가 흩어질 수 있음**.

```
  리프가 넘친 뒤 (개념)
           [ 30 ]                    ← 부모: 승격된 키
          /      \
   [10, 20]      [40, 50]            ← 30은 이 리프들에 “데이터 한 벌”로 남지 않을 수 있음
```

**B+ tree:** 리프를 나눈 뒤 **오른쪽 리프의 첫 키가 부모에 복사**되고, **실제 key/payload는 리프에 그대로** 남음.

```
  리프 split 직후 (개념)
           [ 30 ]                    ← 부모: 오른쪽 리프 첫 키의 “복사본” (separator)
          /      \
   [10, 20]      [30, 40, 50]       ← 30은 오른쪽 리프에도 존재 (range scan이 리프만 보면 됨)
```

---

## 협업 방식

```mermaid
%%{init: {'theme':'base', 'themeVariables': {'fontSize':'20px', 'fontFamily':'system-ui, sans-serif'}}}%%
flowchart TB
    subgraph P1["① 초반"]
        a["B+ 트리 핵심 코드\nline-by-line 분석"]
    end

    subgraph P2["② 전환점"]
        b["전체 뜯기는 시간 비용 ↑\n→ 회의로 범위·역할 조정"]
    end

    subgraph P3["③ 이후"]
        c["구현 시퀀스 기준으로\n연결 지점·전체 흐름 정리"]
    end

    subgraph P4["④ 효과"]
        d["B+ 원리 확정\n→ INSERT · Parser · SELECT · Bench\n한 줄로 연결"]
    end

    P1 --> P2 --> P3 --> P4
```

| 구간  | 방식                                                |
| --- | ------------------------------------------------- |
| 초반  | B+ 트리 핵심 코드 중심으로 line-by-line 분석                  |
| 전환점 | 전체 코드를 모두 뜯는 방식은 시간이 커서 회의 후 범위 조정                |
| 이후  | 구현 시퀀스를 기준으로 전체 흐름과 연결 지점 파악                      |
| 효과  | B+ 트리 원리를 먼저 잡고 INSERT·Parser·SELECT·Bench 흐름을 연결 |


